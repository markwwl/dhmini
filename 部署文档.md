# 健康饮食方案小程序（dhmini）部署文档

> 适用版本：当前 `main`（`c4c0b9b` 及之后）
> 适用范围：后端 API 发布 + 微信小程序上线 + 管理后台托管（含生产环境配置、反向代理、进程守护、安全清单）

---

## 1. 架构与组件

```
                         ┌─────────────────────────────────────────┐
   微信用户  ──HTTPS──▶   │   公网域名 (nginx / caddy 终止 TLS)        │
                         │     /api/*   → 后端 API :24661            │
                         │     /admin/* → 后端 API 静态托管           │
                         │     /uploads/* → 后端 API 图片直出        │
                         └───────────────┬─────────────────────────┘
                                         │ (内网 / 本机转发)
                         ┌───────────────▼─────────────────────────┐
                         │  DietPlan.Api (ASP.NET Core 8, net8.0)   │
                         │  Kestrel 监听 127.0.0.1:24661             │
                         │  EF Core 8 → SQL Server                  │
                         └───────────────┬─────────────────────────┘
                                         │
                         ┌───────────────▼─────────────────────────┐
                         │  SQL Server（本项目实测 2019，≥2012 即可）│
                         └─────────────────────────────────────────┘

   运营人员 ──HTTPS──▶ 管理后台 SPA (/admin/)  ← 调 /api/admin/*
   小程序   ──HTTPS──▶ 后端 API (/api/auth/wx-login 等)
```

| 组件 | 技术 | 说明 |
| --- | --- | --- |
| 后端 API | ASP.NET Core 8 / Clean Architecture，目标框架 `net8.0` | 启动工程 `DietPlanApp/backend/src/DietPlan.Api`，默认监听 `0.0.0.0:24661` |
| 小程序 | 微信原生小程序（appid `wx229e9895042d08a3`） | 8 个页面，`utils/config.js` 按 `envVersion` 切换开发/生产 BASE |
| 管理后台 | Vue3 + Element Plus（CDN 免编译 SPA） | 由 API 的 `wwwroot/admin` 直接托管，**源码不在本仓库**（见第 7 节） |
| 数据 | EF Core 8 + SQL Server | 启动 `EnsureCreated()` 自动建库并植入 `admin/admin123` |

---

## 2. 部署前置条件

| 项 | 要求 |
| --- | --- |
| 运行时/SDK | .NET 8 SDK（发布用）或 .NET 8 Runtime（仅运行已发布产物）；本项目目标框架 `net8.0` |
| 数据库 | SQL Server 2012 及以上（实测 2019）；需允许 TCP 连接 |
| 公网域名 | 微信强制要求 **HTTPS**，且域名需 ICP 备案 |
| 小程序凭证 | 微信公众平台「开发管理 → 开发设置」中的 AppID、AppSecret |
| 服务器域名备案 | 微信「服务器域名」中的 request / uploadFile / downloadFile 须为已备案 HTTPS 域名 |
| 反向代理（推荐） | nginx 或 caddy，用于统一终止 TLS 并转发到后端 24661 端口 |

> 本项目不使用 Docker。按约定以本地进程（可注册为系统服务）方式部署。

---

## 3. 后端发布

### 3.1 取代码

```bash
git clone https://github.com/markwwl/dhmini.git
cd dhmini
```

> 仓库已 `.gitignore` 排除 `appsettings.json`、`*.user` 等，本地密钥不会入库。

### 3.2 生成生产 JWT 密钥

生产环境 `Jwt:Key` 缺失会**拒绝启动**（防伪造 token）。生成强随机密钥：

```bash
python -c "import secrets;print(secrets.token_hex(32))"
# 或：openssl rand -hex 32
```

### 3.3 发布（Release）

后端无 `.sln`，直接对 Api 工程发布：

```bash
# Windows（PowerShell）
dotnet publish DietPlanApp/backend/src/DietPlan.Api/DietPlan.Api.csproj -c Release -o ./publish/api

# Linux
dotnet publish DietPlanApp/backend/src/DietPlan.Api/DietPlan.Api.csproj -c Release -o ./publish/api
```

发布产物在 `./publish/api`，含 `DietPlan.Api.dll` 与 `wwwroot/`。

#### 3.3.1 两种发布形态（按服务器情况二选一）

| 形态 | 命令 | 产物 | 适用 |
| --- | --- | --- | --- |
| **框架依赖**（默认，跨平台，体积小） | `dotnet publish ... -c Release -o ./publish/api` | `publish/api/`（约几 MB） | 服务器已装 **.NET 8 Runtime**；Windows/Linux 通用 |
| **自包含**（免装运行时，Windows 首选） | `dotnet publish ... -c Release -r win-x64 --self-contained true -o ./publish/api-win-x64` | `publish/api-win-x64/`（约 90 MB，含 `DietPlan.Api.exe`） | Windows Server **无需安装 .NET**；Linux 把 `-r` 换成 `linux-x64` |

> 本次已预编译产出两个可直接部署的包（见仓库根 `publish/`，已加入 `.gitignore` 不入库）：
> - `publish/api/` —— 框架依赖（Linux/Windows 通用，需 .NET 8 Runtime）
> - `publish/api-win-x64/` —— Windows x64 自包含（双击/服务化即跑，免装运行时）
>
> **自包含形态的运行差异**：入口是可执行文件，服务化时 `nssm` 的 `Application` 直接指向 `api-win-x64\DietPlan.Api.exe`，**不需要**再传 `AppParameters` 指向 dll，也无需系统装 dotnet。

### 3.4 生产配置 `appsettings.json`

发布目录里**没有** `appsettings.json`（被 gitignore）。从模板复制并填真实值：

```bash
cp appsettings.example.json ./publish/api/appsettings.json
```

按生产填（示例为 SQL Server 2019 + 微信真实凭证）：

```json
{
  "ConnectionStrings": {
    "Default": "Server=JWSGWWL\\MS2019;Database=DietPlanDB;User Id=dietplan;Password=<强密码>;MultipleActiveResultSets=true;TrustServerCertificate=True"
  },
  "Jwt": {
    "Key": "上一步生成的64位十六进制强随机密钥",
    "Issuer": "DietPlan",
    "Audience": "DietPlan"
  },
  "Wx": {
    "AppId": "wx229e9895042d08a3",
    "AppSecret": "微信公众平台真实 AppSecret（切勿提交到仓库）"
  },
  "Database": { "Provider": "SqlServer" },
  "Logging": {
    "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" }
  },
  "AllowedHosts": "*"
}
```

要点：
- `Jwt:Key` 必须填强随机值，否则生产启动即报错。
- `Wx:AppId/AppSecret` 须与微信公众平台一致，否则 `wx.login` 静默登录换 openid 失败。
- `Database:Provider` 省略时默认 `SqlServer`；仅本地冒烟才用 `InMemory`。
- 连接串建议用**专用 SQL 账号**而非 `Trusted_Connection`（服务器上 Service 账户未必有权）。

### 3.5 运行方式

#### 方式 A：Kestrel 直跑（仅测试/内网）

```bash
set ASPNETCORE_ENVIRONMENT=Production      # 关闭 Swagger（Swagger 仅 Development 启用）
set ASPNETCORE_URLS=http://127.0.0.1:24661  # 仅监听本机，避免端口直接暴露公网
cd ./publish/api
dotnet DietPlan.Api.dll
```

> `Program.cs` 内 `UseUrls("http://0.0.0.0:24661")` 是**最低优先级**，会被环境变量 `ASPNETCORE_URLS` 覆盖，因此设 `127.0.0.1:24661` 可让其只听本机，由前置反向代理转发。

#### 方式 B：反向代理 + 进程守护（生产推荐，见第 4、5 节）

### 3.6 进程守护

**Windows（用 nssm 注册为服务，开机自启 + 崩溃重启）**

```powershell
nssm install DietPlanApi "C:\Program Files\dotnet\dotnet.exe"
nssm set DietPlanApi AppParameters "C:\apps\dhmini\publish\api\DietPlan.Api.dll"
nssm set DietPlanApi AppDirectory "C:\apps\dhmini\publish\api"
nssm set DietPlanApi AppEnvironmentExtra "ASPNETCORE_ENVIRONMENT=Production" "ASPNETCORE_URLS=http://127.0.0.1:24661"
nssm set DietPlanApi DisplayName "DietPlan API"
nssm start DietPlanApi
```

> 也可用系统 `sc create`，但 nssm 自带崩溃重启更省心。

**Linux（systemd）**

```ini
# /etc/systemd/system/dietplan-api.service
[Unit]
Description=DietPlan API
After=network.target

[Service]
WorkingDirectory=/opt/dhmini/publish/api
ExecStart=/usr/bin/dotnet /opt/dhmini/publish/api/DietPlan.Api.dll
Environment=ASPNETCORE_ENVIRONMENT=Production
Environment=ASPNETCORE_URLS=http://127.0.0.1:24661
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload && sudo systemctl enable --now dietplan-api
```

---

## 4. 反向代理（nginx 示例，统一终止 TLS）

微信要求前端访问必须为 HTTPS，因此由 nginx 对外提供 443，内部转发到本机 24661。

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate     /etc/nginx/ssl/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/your-domain.com/privkey.pem;

    # 前端/小程序调用的 API
    location /api/ {
        proxy_pass         http://127.0.0.1:24661;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # 管理后台 SPA
    location /admin/ {
        proxy_pass         http://127.0.0.1:24661;
        proxy_set_header   Host $host;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # 图片直出
    location /uploads/ {
        proxy_pass         http://127.0.0.1:24661;
    }
}
```

> 建议：在 `Program.cs` 中于 `UseAuthentication()` 之前增加 `app.UseForwardedHeaders()`（信任 `X-Forwarded-Proto`），以便后端正确识别 HTTPS 来源（当前代码未启用，若后续需基于 scheme 做判断请补上）。

防火墙：公网只放行 443，24661 端口仅本机/内网可达。

---

## 5. 数据库

- 首次启动后端时 `db.Database.EnsureCreated()` 会**自动建库建表**（本项目无迁移脚本，模型变更需手动 DDL）。
- 若库为空，自动植入后台账号 `admin / admin123`（见 `Program.cs` 启动初始化段）。
- 连接串见第 3.4 节；SQL Server 需开启 TCP/IP 并允许应用账号登录。

> ⚠️ **上线前必须修改 `admin` 密码**（在管理后台或库内 `AdminUsers` 表），默认的 `admin123` 仅用于初始化。

---

## 6. 小程序发布上线

### 6.1 修改生产地址

编辑 `DietPlanApp/miniprogram/utils/config.js`，把 `PROD_BASE` 改成你的真实 HTTPS 域名：

```js
const DEV_BASE  = 'http://192.168.3.100:24661';
const PROD_BASE = 'https://your-domain.com'; // 改为真实已备案 HTTPS 域名
```

小程序按 `envVersion` 自动选：`release`（正式版）走 `PROD_BASE`，`develop`/`trial` 走 `DEV_BASE`，无需手动切换。

### 6.2 微信公众平台配置服务器域名

登录 [微信公众平台](https://mp.weixin.qq.com) →「开发管理 → 开发设置 → 服务器域名」，将以下域名均设为 `https://your-domain.com`（并去掉 `http://` 与端口）：

- request 合法域名：`https://your-domain.com`
- uploadFile 合法域名：`https://your-domain.com`
- downloadFile 合法域名：`https://your-domain.com`
- （如用到 socket）socket 合法域名同理

> `project.config.json` 中 `"urlCheck": false` 仅本地开发者工具联调生效；**上传/体验版/正式版强制校验合法域名**，必须配置且为 HTTPS。

### 6.3 上传与发布

1. 微信开发者工具 → 导入项目 → 目录选 `DietPlanApp/miniprogram`（appid 已为 `wx229e9895042d08a3`，如不符请改）。
2. 详情 → 本地设置确认 `urlCheck` 关闭仅用于本机调试；上传代码前确保 `PROD_BASE` 已改为生产域名。
3. 点「上传」填写版本号与备注 → 在公众平台「版本管理」提交审核 → 审核通过后发布。
4. 体验版可先发「体验版」用真机验证静默登录、方案浏览、打卡、视频上新角标等。

---

## 7. 管理后台托管（重要）

README 描述为「由 API 的 `wwwroot/admin` 直接托管」，但**当前仓库 `DietPlan.Api/wwwroot/admin` 目录并不存在**——管理后台前端（Vue3 + Element Plus CDN SPA）源码未随仓库提供。

部署步骤：
1. 从原作者/内部仓库获取已构建的管理后台产物（至少含 `index.html` 及依赖资源）。
2. 放入 `DietPlanApp/backend/src/DietPlan.Api/wwwroot/admin/`，再执行第 3.3 节发布；或直接拷到已发布目录 `./publish/api/wwwroot/admin/`。
3. 访问 `https://your-domain.com/admin/` 即可使用（后端 `/api/admin/*` 接口已在代码中就绪）。

> 若未放入 `wwwroot/admin`，访问 `/admin/` 会 404。后台「内容块 → 上新勾选」等运营功能依赖此前端，调现有 `/api/admin/content-blocks` 即可。

---

## 8. 安全清单（上线前逐项核对）

- [ ] `appsettings.json` 含真实密钥，且**未提交到仓库**（已 gitignore 验证通过）。
- [ ] `Jwt:Key` 为强随机（64 位十六进制），不与开发环境相同。
- [ ] `Wx:AppSecret` 正确且与公众平台一致。
- [ ] 默认后台账号 `admin/admin123` 已修改密码。
- [ ] 生产以 `ASPNETCORE_ENVIRONMENT=Production` 运行，Swagger 已关闭（避免接口暴露）。
- [ ] 后端仅监听 `127.0.0.1:24661`，公网仅 443 出网，24661 不对外。
- [ ] 微信服务器域名已配置且为 HTTPS + 已备案。
- [ ] TLS 证书有效（nginx/caddy 终止），定期续期。
- [ ] 进程守护已配置（nssm / systemd），崩溃可自动重启。
- [ ] 数据库已建库并连通，连接串使用专用 SQL 账号。

---

## 9. 运维与排错

| 现象 | 排查 |
| --- | --- |
| 后端启动报 `缺少 Jwt:Key` | 生产未设 `Jwt:Key`，按 3.2/3.4 生成填入 |
| 小程序登录失败 / 401 | `Wx:AppId/AppSecret` 不符；或服务器域名未配 HTTPS |
| 访问 `/admin/` 404 | `wwwroot/admin` 未放入产物（见第 7 节） |
| 502 Bad Gateway | nginx 转发到 24661，但后端未起或 `ASPNETCORE_URLS` 非 `127.0.0.1:24661` |
| 数据库连不上 | SQL Server TCP 未开 / 账号无权限 / 连接串错；确认 `Database:Provider=SqlServer` |
| Swagger 仍在生产可见 | 确认环境变量 `ASPNETCORE_ENVIRONMENT=Production` 已生效 |

**健康检查 / 日志**
- 进程日志：nssm 日志在 `nssm` 配置目录；systemd 用 `journalctl -u dietplan-api -f`。
- 接口冒烟：生产可临时开 Development 看 Swagger，或用第 2 节克隆的 `e2e_smoke.py`（需配 InMemory）做本地链路校验。

---

## 附：一键发布速查（Windows Server 2019 示例）

```powershell
# 1) 发布（框架依赖；若要免装运行时改用下方自包含命令）
dotnet publish DietPlanApp/backend/src/DietPlan.Api/DietPlan.Api.csproj -c Release -o C:\apps\dhmini\publish\api

# 1b) 自包含（Windows x64，无需装 .NET 8 Runtime）：
# dotnet publish DietPlanApp/backend/src/DietPlan.Api/DietPlan.Api.csproj -c Release -r win-x64 --self-contained true -o C:\apps\dhmini\publish\api-win-x64

# 2) 放生产 appsettings.json（手动填密钥/连接串/Wx 凭证）

# 3) 注册服务（nssm，已设 Production + 仅本机监听）
nssm install DietPlanApi "C:\Program Files\dotnet\dotnet.exe"
nssm set DietPlanApi AppParameters "C:\apps\dhmini\publish\api\DietPlan.Api.dll"
nssm set DietPlanApi AppDirectory "C:\apps\dhmini\publish\api"
nssm set DietPlanApi AppEnvironmentExtra "ASPNETCORE_ENVIRONMENT=Production" "ASPNETCORE_URLS=http://127.0.0.1:24661"
nssm start DietPlanApi

# 4) nginx 配置 443 → 127.0.0.1:24661，放行 443、封闭 24661

# 5) 小程序：改 config.js 的 PROD_BASE，配服务器域名，上传发布

# 6) 运营：放 wwwroot/admin 产物到 publish\api\wwwroot\admin，访问 /admin/ 改 admin 密码
```
