# 健康饮食方案小程序 · 代码审核与优化建议

> 对照文件：`output/20260911-dietplan-reqdoc/stage1/final_draft.md`（需求 V1.0）
> 审查范围：`DietPlanApp/miniprogram`（微信原生端全部 8 个页面 + 请求层）
> 审查日期：2026-09-12

## 结论速览

整体完成度不错：**四级页面 L1→L4 流转完整、换菜候选组约束、打卡三粒度（菜/餐/日）、收藏本地化、统计（累计/连续/30 天）、视频秀、JWT+401 自动续登**均已实现，骨架扎实。

但对照需求文档，仍有 **1 项合规硬伤、3 处需求功能缺失、2 个体验/正确性问题、若干非功能与细节** 需要优化。按严重程度分级如下。

---

## 🔴 高（不修会影响提审或主流程）

### 1. 隐私协议弹窗缺失（需求 6·合规）——提审会被拒
- 现状：`app.js` 的 `onLaunch` 直接 `wx.login` 静默登录，无隐私授权流程。
- 风险：微信要求「首次启动展示隐私协议弹窗」并调用 `wx.requirePrivacyAuthorize` 取得同意后才能获取用户信息，否则审核驳回。
- 建议：在 `onLaunch` 增加隐私授权判断——未同意则弹 `privacy-popup` 组件，同意后再静默登录；同意状态存 `wx.setStorageSync('privacy_accepted')`。

### 2. 更换菜品返回后当日食谱不刷新（需求 4.2「更换后即时刷新」）——功能缺陷
- 现状：`day.js` 仅在 `onLoad` 调用 `loadDay()`；从 `replace` 页 `navigateBack` 回 L2 时，`day` 页面不会重建，`onShow` 未定义，所以换菜结果不刷新。
- 验证：`day.wxml` 28 行 `更换菜品 ›` 进入 L4，确认后 `replace.js` `navigateBack`，回到 L2 看不到新菜。
- 建议：在 `day.js` 增加 `onShow(){ this.loadDay(); }`。注意 `onLoad` 已加载过，`onShow` 重复加载可接受（接口很快）；若担心抖动，可加标志位仅在 `navigateBack` 时刷新。

---

## 🟠 中（需求文档明确要求但未实现）

### 3. 首页缺「健康资讯列表」（需求 4.6）
- 现状：`home.js` 只拉取 `banner` 与 `plans`，没有拉取 `type=news`/`articles` 的资讯块，`home.wxml` 也无对应渲染。
- 建议：后端 `ContentBlocks` 已有 `type` 维度，补 `request('/api/content/blocks?type=news')`，首页卡片下方渲染图文列表（点击进 webview 或富文本页）。

### 4. L1 阶段导航缺「滚动高亮当前阶段」（需求 4.1）
- 现状：`plan.js` 的 `tapStage` 只做点击跳转、点击高亮；页面滚动时 `activeStage` 不变，需求要求「滚动时高亮当前阶段」。
- 建议：用 `wx.createIntersectionObserver` 监听各 `#stage-{{index}}` 进入视口，动态 `setData({ activeStage })`，实现 scroll-spy。

### 5. L2 缺「今日食谱所需食材 ›」入口（需求 4.2 右上角）
- 现状：`day.wxml` 的「营养食谱」区只有标题，无右侧入口；前端未聚合当天食材。
- 建议：方案 A（前端聚合）遍历 `day.meals[].slots[].ingredientsSummary` 去重汇总，弹层展示；方案 B（后端）新增 `GET /api/weeks/{id}/days/{dayNo}/ingredients` 返回结构化清单，便于后续「采购清单导出/分享」（需求 9·待确认项 5）。推荐方案 B。

### 6. L2 缺右下角「方案解读」悬浮入口（需求 4.2）
- 现状：`day.wxml` 无 fab 悬浮按钮。
- 建议：加一个 `position:fixed` 的圆形/胶囊按钮，一期按需求挂「图文」（复用 `showPlanDetail` 的富文本），待确认项 2 拍板后再切视频。

### 7. L2 顶部标题缺「方案名」前缀（需求 4.2 标题格式）
- 现状：`day.js:22` 标题为 `${stage}（${label}）`，需求格式为「方案名+阶段+周」。
- 建议：拼接 `day.planName`（需 `/meals` 接口返回 planName，或页面参数带来）。

### 8. 打卡「仅限当日」未校验（需求 4.5）
- 现状：`day.js` 的 `checkin`/`checkinMeal` 只传 `weekId/dayNo/slotId`，不判断所选天是否为当天；历史天也能打卡。
- 关联：需求写「打卡仅限当日，历史天只读不可补打卡」，但「是否允许补打卡」是**待确认项 1**。
- 建议：先催拍板待确认项 1；若确定不可补打卡，则后端按 `date=今天` 校验（返回 400），前端对历史天隐藏打卡按钮、仅展示已打卡标识。

### 9. 「我的」页缺「收藏的菜品」聚合列表（需求 4.6）
- 现状：`dish.js` 收藏仅写本地 `liked_dish_{id}`；`mine.js`/`mine.wxml` 无收藏列表。
- 建议：一期若保持本地，可在 `mine` 读取本地 `liked_*` 键集合展示；待确认项 3 若决定服务端持久化，则改用 `GET /api/users/me/likes`。

---

## 🟡 低（体验 / 非功能 / 细节）

### 10. 默认落地 Tab 应为「方案」（需求 3）
- 现状：`app.json` 中 `pages` 首页排第一成为默认进入页，需求规定「方案为默认主 Tab」。
- 建议：把 `pages/plan/plan` 放到 `pages` 数组首位（TabBar 顺序同步），或首页仅做中转。

### 11. L1 阶段导航应为「左侧竖向」（需求 4.1）
- 现状：`plan.wxml` 的 `nav` 是顶部横向条；需求为左侧竖向导航条。
- 建议：改为 `position:fixed; left:0` 的竖向条（`width:160rpx`），右侧内容留白，更贴合竞品形态与「小屏适配」。

### 12. 视频秀「上新」角标未展示（需求 4.6）
- 现状：`videos.wxml` 未读 `isNew`/开关字段。
- 建议：资讯/视频块带 `isNew` 时卡片左上角显示「上新」红标。

### 13. 图片懒加载 & 本地缓存（需求 6·性能）
- 现状：各页 `<image>` 未加 `lazy-load`；`request` 层无缓存。
- 建议：列表/长图 `<image>` 加 `lazy-load`；`banner`、`plans` 树等低频数据在 `request` 层加 5 分钟内存缓存（符合「小程序端做懒加载与本地缓存」）。

### 14. 真机地址硬编码（工程规范）
- 现状：`utils/request.js:3` 写死 `http://192.168.3.100:5000`，会随提交进仓库；换网段就要改代码。
- 建议：抽成 `config.js` 导出 `BASE`，或用 `wx.getAccountInfoSync()` 区分 dev/prod 环境自动选地址；上线切换 https 域名。

### 15. 微信真实头像/昵称未获取（需求 4.6）
- 现状：`wx.login` 只能换 openid，昵称/头像来自服务端兜底，故「我的」显示默认「健康饮食用户」，头像用首字母占位。
- 建议：用「头像昵称填写」能力（`button open-type="chooseAvatar"` + `input type="nickname"`）让用户授权，或在隐私合规前提下用 `wx.getUserProfile`（基础库已收紧，需走新版头像昵称组件）。

---

## ✅ 已较好实现（保持）

- 四级页面 L1→L4 完整流转，换菜候选组约束由后端 `/api/dish-slots/{id}/candidates` 保障（需求 4.4 营养结构不被破坏）。
- 打卡三粒度：单菜打卡 / 餐次一键打卡 / 整日打卡，底层统一为菜品明细（需求 4.5）。
- 收藏本地化符合一期定位（需求待确认项 3）。
- 统计含累计、连续、近 30 天日历（需求 4.6，日历建议后续改月视图网格）。
- JWT 鉴权 + 401 自动静默续登（需求 6·安全）。
- 管理后台 SPA + 四级内容 CRUD 已具备，内容双态、引用校验待对照后台代码复核。

---

## 建议优先级（若排期）

1. **必做（提审/主流程）**：#1 隐私弹窗 ✅、#2 换菜刷新 ✅、#8 打卡日期校验（待确认项 1 已拍板=允许补打卡，已按「计划天+菜品」身份解耦方案落地 ✅）。
2. **应做（需求对齐）**：#3 资讯列表、#4 滚动高亮、#5 食材入口、#6 方案解读 fab、#9 收藏列表、#10 默认 Tab。
3. **选做（体验/规范）**：#7 标题前缀、#11 竖向导航、#12 上新角标、#13 懒加载缓存、#14 地址配置化、#15 真实头像昵称。

> 待确认项（需求 9 未拍板，建议尽快定，避免返工）：~~补打卡~~（**已拍板：允许补打卡**）、方案解读图文/视频、收藏服务端化、图片存储（磁盘/OSS）、食材清单导出分享。

---

## 可立即落地的「快速修复包」
以下改动成本低、收益高，需要我直接改代码的话说一声：
- `app.js` 加隐私授权 + 同意后再登录
- `day.js` 加 `onShow` 刷新
- `plan.js` 加 `IntersectionObserver` 滚动高亮
- `home.js` 补 `news` 资讯块拉取与渲染
- `request.js` 抽 `config.js` 环境化 BASE

---

## 快速修复包实施记录（2026-09-12 已落地）

| # | 项 | 改动文件 | 说明 |
|---|----|---------|------|
| 1 | 隐私授权 | `app.js`、`app.json`（加 `__usePrivacyCheck__:true`） | 启动先 `wx.getPrivacySetting`→需授权则 `wx.requirePrivacyAuthorize` 弹官方协议页，同意后再静默登录；拒绝也先放行（后续隐私接口微信会再拦截） |
| 2 | 换菜返回刷新 | `pages/day/day.js` | 移除 `onLoad` 里的 `loadDay`，改由 `onShow` 调用，从 L4/L3 `navigateBack` 回 L2 时自动重载当日食谱 |
| 3 | 滚动高亮阶段 | `pages/plan/plan.js`、`plan.wxml` | `plan.wxml` 阶段块包 `<view id="stage-{{index}}" class="stage-block">`；`plan.js` 数据就绪后用 `IntersectionObserver` 监听 `.stage-block` 进入视口，动态更新 `activeStage`；`onUnload` 断开 observer |
| 4 | 首页健康资讯 | `pages/home/home.js`、`home.wxml`、`home.wxss` | `home.js` 用 `Promise.all` 并行拉 `banner`/`news`/`plans`；`home.wxml` 在方案卡下渲染「健康资讯」列表（title+image），`home.wxss` 补 `.news-*` 样式 |
| 5 | 地址环境化 | `utils/config.js`（新增）、`utils/request.js` | 新增 `config.js` 按 `wx.getAccountInfoSync().miniProgram.envVersion` 自动选 `DEV_BASE`/`PROD_BASE`，`request.js` 的 `BASE` 改从 config 引入，消除硬编码 |

校验：`node --check` 6 个改动 JS 文件全部 OK；后端 `/api/content/blocks?type=news`、`?type=banner` 均 200（当前库为空数组，待后台录入资讯即显示）。

> 未纳入本次快速包（见上方中/低优先级，需排期）：打卡仅限当日后端校验、L2 今日食材入口/方案解读 fab、我的收藏列表、默认 Tab 改方案、L1 左侧竖向导航、视频上新角标、图片懒加载/缓存、微信真实头像昵称。

---

## 第二批实施记录（2026-09-12，剩余优化项落地）

> 本批覆盖建议清单中除 #8、#12 外的全部剩余项；#11 经核代码已满足，无需改动。

| # | 项 | 改动文件 | 说明 |
|---|----|---------|------|
| 10 | 默认落地 Tab=方案 | `app.json` | `pages` 与 `tabBar.list` 首位均改为 `pages/plan/plan`，启动默认进入「方案」主 Tab（需求 3） |
| 7 | L2 标题加方案名前缀 | `pages/plan/plan.js`、`pages/day/day.js` | `goDay` 携带 `plan.name`；`day.onLoad` 拼接标题为「方案名 · 阶段（周）」（需求 4.2 标题格式） |
| 5 | L2 今日食材入口 | `pages/day/day.js`、`day.wxml`、`day.wxss` | `loadDay` 聚合当日三餐 `ingredientsSummary` 按 `、，,；;` 切分去重；营养食谱区右上「今日食材 ›」入口 + 弹层展示采购清单（方案 A 前端聚合，零后端改动，需求 4.2） |
| 6 | L2 方案解读悬浮入口 | `pages/day/day.wxml`、`day.wxss` | 右下角固定胶囊 `fab`「方案解读」，复用 `showPlanDetail` 富文本浮层（需求 4.2，待确认项 2 拍板后再切视频） |
| 9 | 我的收藏菜品列表 | `pages/dish/dish.js`、`pages/mine/mine.js`、`mine.wxml`、`mine.wxss` | `dish.js` 收藏改写结构化 `{id,name,image,subtitle}` 并维护索引键 `fav_dish_ids`；「我的」页渲染收藏聚合列表，点击进 L3（需求 4.6，一期本地化） |
| 15 | 微信真实头像昵称 | `pages/mine/mine.js`、`mine.wxml`、`mine.wxss` | `button open-type="chooseAvatar"` 取头像 + `input type="nickname"` 取昵称，存 `wx_profile` 本地并显示（需求 4.6，待确认项 3 决定是否服务端化） |
| 13 | 图片懒加载 + 接口本地缓存 | `utils/request.js`、`home/plan` 接口、`home/videos/day/plan/dish` 的 `<image>` | `request` 增加可选 `cacheMs` GET 缓存（仅显式 opt-in 的低频接口：banner/news/plans 树，5 分钟 TTL，避开打卡实时数据）；列表图统一加 `lazy-load`（需求 6·性能） |
| 11 | L1 左侧竖向导航 | —（无需改动） | 复核 `plan.wxss` 当前已是 `display:flex` + 左侧 `140rpx` sticky 竖向导航，原审核「顶部横向」判断与现状不符，已满足需求 4.1 |

**未做（依赖产品拍板 / 缺数据字段，已在需求文档待确认项标注）：**
- **#12 视频「上新」角标**：`ContentBlock` 实体无 `IsNew` 字段，需后端加列 + EF 迁移 + 后台录入开关；属较轻但需动库，建议与后台「内容块上新标记」一并处理。

## 第三批实施记录（2026-09-12，待确认项 1 拍板：允许补打卡）

> 用户已确认：**允许补打卡**（待确认项 1 结论）。据此将打卡身份从「真实自然日」改为「计划天 + 菜品」，与日期解耦，使历史天可补打且持久化。

| # | 项 | 改动文件 | 说明 |
|---|----|---------|------|
| 8 | 允许补打卡（后端身份解耦） | `AppDbContext.cs`、`CheckinService.cs`、`ContentService.cs`、`UserAndContent.cs` | ① 唯一索引由 `(UserId,Date,DishId)` 改为 `(UserId,WeekId,DayNo,DishId)`，避免「同一天补打两个含相同菜品的计划天」被唯一约束静默丢弃；② `InsertCheckinAsync` 去重改为按「计划天+菜品」；③ `GetDayMealsAsync` 的 `checked` 由 `Date==today` 改为按「计划天+菜品」，确保今天补打的历史计划天次日仍正确显示已打卡；④ `Date` 字段语义明确为「实际打卡自然日」（用于近30天日历/连续打卡统计）。 |

**数据库同步（关键）**：项目用 `EnsureCreated()` 无迁移，改索引不会自动落到已有库。已通过一次性工具对 `JWSGWWL\MS2019` 的 `DietPlanDB` 执行 DDL：删除旧索引 `IX_CheckInRecords_UserId_Date_DishId`、新建 `IX_CheckInRecords_UserId_WeekId_DayNo_DishId`；`AppDbContext` 模型声明已同步，未来全新建库一致。

**端到端验证（注入演示数据后跑 API，已清理）**：day1 打卡鸡蛋 → 重复打卡幂等 → **day2 同天补打卡鸡蛋（同菜品）成功**（旧索引会丢此条）；day1/day2 食谱鸡蛋均 `checked=true`；summary `calendar: 2026-09-12 count=2`（两条独立记录均落库，无丢数据）。验证通过。

**说明**：前端无需改动——`day.js` 打卡本就按 `weekId/dayNo` 发起，无日期门禁；补打卡能力由本次后端身份解耦天然支持。打卡统计（连续/日历）仍以「实际自然日」计，符合「允许补打卡」语义。

## 第四批实施记录（2026-09-12，#12 视频上新角标）

> 用户要求「接着上」，完成最后一项 #12：视频秀列表「上新」角标。涉及后端字段 + 现有库加列 + 小程序角标 + 后台开关链路。

| # | 项 | 改动文件 | 说明 |
|---|----|---------|------|
| 12 | 视频「上新」角标 | `UserAndContent.cs`、`MiscAdminControllers.cs`、`pages/videos/videos.wxml`、`pages/videos/videos.wxss` | ① 领域实体 `ContentBlock` 增 `IsNew(bool, 默认 false)`；② client 端 `ContentController.GetBlocks` 直接序列化实体，`IsNew` 自动以 camelCase `isNew` 下发；③ admin 端 `AdminContentBlocksController.Update` 补 `block.IsNew = input.IsNew`，后台开关可写；④ 小程序 `videos.wxml` 按 `item.isNew` 显示右上角红色「上新」角标，`videos.wxss` 加 `.vcard{position:relative}` + `.badge-new` 样式，`videos.js` 无需改动（数据直接绑定）。 |

**数据库同步**：`DietPlanDB` 用 `EnsureCreated()` 无迁移，`ContentBlocks` 表已通过一次性工具 ALTER TABLE 加 `IsNew BIT NOT NULL DEFAULT 0`（`IsNew_PRESENT=True` 已确认）。模型声明已同步，未来全新建库一致。

**端到端验证（已清理）**：admin 登录 → POST 建 `IsNew=true` 视频块 → client `GetBlocks?type=video` 返回 `isNew=true`（证明小程序可拿到）→ admin `PUT` 改 `IsNew=false` 回写成功 → client 再次确认 `isNew=false` → DELETE 清理，库恢复空。验证通过。

**后台开关说明**：admin Web API（`/api/admin/content-blocks` 的 POST/PUT）已完整支持 `isNew` 读写，运营可在管理后台内容块编辑界面勾选「上新」；本仓库 `wwwroot/admin` 未携带管理后台前端源码，需在管理后台前端补「内容块 → 上新勾选」UI（纯前端，调现有 admin API 即可）。小程序侧角标渲染需在微信开发者工具/真机联调复核。
